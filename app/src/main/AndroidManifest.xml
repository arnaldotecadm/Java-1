<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.pucgo.atividade">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Atividade">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Menu"></activity>
        <activity android:name=".SorteadorDeValores"></activity>
        <activity android:name=".JogoJankenpon"></activity>
        <activity android:name=".InversorDePalavras"></activity>
        <activity android:name=".RegistroDeEventos"></activity>
        <activity android:name=".ResultadoTexto"></activity>
    </application>

</manifest>